//server config

const config = {
    app: {
        port: 8000
    },
    db: {
        host: "localhost",
        port: 8000,
        name: 'fitFriends'
    }
};

module.exports = {
    config: config
}
